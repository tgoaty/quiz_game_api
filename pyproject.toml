[project]
name = "quiz-game-api"
version = "0.1.0"
description = "quiz game api for my frontend friend"
authors = [
    { name = "Nikolay Pushkin", email = "adwedelf@gmail.com" }
]
readme = "README.md"
requires-python = ">=3.12"


[tool.poetry]
packages = [
    { include = "app" }
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
